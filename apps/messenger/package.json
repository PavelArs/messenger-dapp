{
  "name": "contracts",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "compile": "hardhat compile",
    "clean": "hardhat clean",
    "dev": "hardhat node && npm run deploy:local",
    "build": "npm run clean && npm run compile",
    "test": "hardhat test",
    "format": "",
    "deploy:local": "hardhat ignition deploy ignition/modules/Messenger.ts --network localhost",
    "deploy:sepolia": "hardhat ignition deploy ignition/modules/Messenger.ts --network sepolia"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "description": "",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox-viem": "^3.0.0",
    "dotenv": "^16.4.7",
    "hardhat": "^2.22.19"
  }
}
